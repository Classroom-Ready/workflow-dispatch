# Test fixture dispatched by build-test.yaml's live-dispatch self-tests via this action.
name: Failing

on:
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Fail
        run: exit 1
